alterado:
	memoria de instrucao
        memoria de dados
	main control
        alu controls



testar clock data memory pra ver se o sw depois do lw funciona
corretamente e pega o dado atualizado, se nao ligar o data_memory
a um novo clock 2x mais rapido

na hora do la do lw e sw, usar um rotulo com o valor direto do
endereço

o bit 30, assumindo o valor f4 do alu control, so importa para diferenciar
a subtração da adição, isso no formato R. Talvez fazer um if formato r, ai sim levar
em consideração o bit f4, pq em outras operações o bit 30 nao é parte do f7,
mas sim de um imediato ou outra coisa, oq significa que a operação pode mudar
por um bit arbitrário

suporte: auipc?, beq, lw, sw, 
	 formato i - addi?, slti?
         formato r - add, sub, slt?, or?, and?


vcom -2008 datapath/banco-de-registradores/*.vhd
vcom -2008 datapath/somador/*.vhd
vcom -2008 datapath/ula/*.vhd
vcom -2008 datapath/*.vhd
vcom -2008 controlador/*.vhd
vcom -2008 *.vhd
vsim tb_rv32_top
add wave *